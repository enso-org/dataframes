import Std.Foreign.C.Value
import Std.Foreign

def scanf x:
    cString = CString.fromText x
    double = lookupSymbol "scanf" "luna_utils" . call CDouble [cString . toCArg]
    cString.free
    double.toReal
